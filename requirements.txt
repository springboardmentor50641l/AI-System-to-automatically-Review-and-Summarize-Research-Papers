# Core libraries for Milestone 1
requests==2.31.0          # For API calls
python-dotenv==1.0.0      # For managing API keys
tqdm==4.66.1              # For progress bars 
pandas==2.0.3             # For data handling (CSV/Excel)